import { MobileValidatorsDirective } from './mobile-validators.directive';

describe('MobileValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
